The objects folder is needed for the `utils.isGitDirectory(..)` check.

This README file is needed in order to commit objects folder to the repository. It's hard or impossible to commit an empty folder.
